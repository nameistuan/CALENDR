/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{js,jsx,ts,tsx}'], // tell Tailwind where to look for classes
  theme: {
    extend: {}, // we can add custom colors, fonts later
  },
  plugins: [],
}
